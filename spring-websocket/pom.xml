<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>nz.net.osnz</groupId>
        <artifactId>osnz-groovy-parent</artifactId>
        <version>1.3</version>
    </parent>

    <groupId>nz.net.osnz.train</groupId>
    <artifactId>spring-websocket</artifactId>
    <version>1.1-SNAPSHOT</version>
    <packaging>war</packaging>

    <name>Train WebSocket</name>
    <description>Train Websocket</description>
    <url>http://www.osnz.net.nz</url>

    <developers>
        <developer>
            <name>Kefeng Deng</name>
            <email>deng@51any.com</email>
        </developer>
    </developers>

    <scm>
        <url>https://git.oschina.net/kdeng/train-websocket.git</url>
        <connection>scm:git:git@git.oschina.net:kdeng/train-websocket.git</connection>
        <developerConnection>scm:git:git@git.oschina.net:kdeng/train-websocket.git</developerConnection>
    </scm>

    <properties>
        <jetty.maven.plugin.version>8.1.10.v20130312</jetty.maven.plugin.version>
        <sonar.language>groovy</sonar.language>
    </properties>

    <dependencies>

        <dependency>
            <groupId>nz.net.osnz.composite</groupId>
            <artifactId>composite-logging</artifactId>
            <version>[1,)</version>
        </dependency>

        <dependency>
            <groupId>nz.net.osnz.composite</groupId>
            <artifactId>composite-groovy</artifactId>
            <version>[1,)</version>
        </dependency>

        <dependency>
            <groupId>nz.net.osnz.composite</groupId>
            <artifactId>composite-spring-webmvc</artifactId>
            <version>[1.3,)</version>
        </dependency>

        <dependency>
            <groupId>javax.websocket</groupId>
            <artifactId>javax.websocket-api</artifactId>
            <version>1.0</version>
        </dependency>

        <dependency>
            <groupId>org.eclipse.jetty</groupId>
            <artifactId>jetty-websocket</artifactId>
            <version>8.1.14.v20131031</version>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.mortbay.jetty</groupId>
                <artifactId>jetty-maven-plugin</artifactId>
                <version>${jetty.maven.plugin.version}</version>
                <configuration>
                    <scanIntervalSeconds>5</scanIntervalSeconds>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>org.eclipse.jetty</groupId>
                        <artifactId>jetty-websocket</artifactId>
                        <version>8.1.14.v20131031</version>
                    </dependency>
                </dependencies>
            </plugin>
        </plugins>
    </build>

</project>